# Screenshots

Regenerate with:

```bash
npm run assets:render
```

## Captions

- `01-campaign-board.png`: A live run keeps the markdown checklist and the current agent activity visible together: plan, run, and watch without a second project database.
- `02-mobile-step-flow.png`: The mobile view keeps the core controls and campaign progress reachable without changing the markdown model.
- `03-library.png`: The library shows the lessons surface and gives paired skills plus manually opened files a shared local switching surface.
