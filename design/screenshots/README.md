# Screenshots

Regenerate with:

```bash
npm run assets:render
```

## Captions

- `01-campaign-board.png`: The board reads checklist state straight from markdown, so the plan file stays the source of truth while the UI gives you a calmer way to execute it.
- `02-mobile-step-flow.png`: The mobile view keeps the current step and bottom navigation reachable without changing the markdown model.
- `03-library.png`: The library shows the lessons surface and gives paired skills plus manually opened files a shared local switching surface.
