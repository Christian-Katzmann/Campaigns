# Trailer

`trailer.mp4` is a local, product-forward preview built from the public demo campaign screenshots.

It starts on the actual Campaigns board so GitHub's auto-generated video poster is useful after the MP4 is uploaded to GitHub user attachments.

Regenerate with:

```bash
npm run assets:render
```
